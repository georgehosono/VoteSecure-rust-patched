# .github/workflows/run-makefile-for-ple-docker-image.yml
# Author: Daniel M. Zimmerman
# Created: 2024-02-14
# Modified: 2026-04-06

name: PLE Docker Image

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]
    paths:
      - 'docker/de-ple-e2eviv/**'
  push:
    branches:
      - 'main'
    paths:
      - 'docker/de-ple-e2eviv/**'
  workflow_dispatch:

jobs:
  build-amd64:
    # Docker image builds are expensive. Run them automatically on pushes
    # to `main`, or on PRs only when the repo variable
    # `DOCKER_BUILD_TEST_BRANCH` matches the PR branch name.
    if: >-
      github.event_name != 'pull_request' ||
      github.head_ref == vars.DOCKER_BUILD_TEST_BRANCH
    name: Build (linux/amd64)
    runs-on: ubuntu-latest
    steps:
      - name: Uninstall Old Docker
        run: |
          sudo apt-get remove -y docker.io docker-doc docker-compose docker-compose-v2 podman-docker containerd runc

      - name: Install Current Docker
        uses: docker/setup-docker-action@v4
        with:
          daemon-config: |
            {
              "features": {
                "containerd-snapshotter": true
              }
            }

      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Run `make image` in `docker/de-ple-e2eviv`
        run: |
          echo "Processing docker image"
          cd docker/de-ple-e2eviv
          IMAGE_PLATFORM=linux/amd64 make image

  build-arm64:
    if: >-
      github.event_name != 'pull_request' ||
      github.head_ref == vars.DOCKER_BUILD_TEST_BRANCH
    name: Build (linux/arm64)
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Uninstall Old Docker
        run: |
          sudo apt-get remove -y docker.io docker-doc docker-compose docker-compose-v2 podman-docker containerd runc

      - name: Install Current Docker
        uses: docker/setup-docker-action@v4
        with:
          daemon-config: |
            {
              "features": {
                "containerd-snapshotter": true
              }
            }

      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Run `make image` in `docker/de-ple-e2eviv`
        run: |
          echo "Processing docker image"
          cd docker/de-ple-e2eviv
          IMAGE_PLATFORM=linux/arm64 make image
