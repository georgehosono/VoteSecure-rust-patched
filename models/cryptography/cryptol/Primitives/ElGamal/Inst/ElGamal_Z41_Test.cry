/**
 * Smoke test of ElGamal’s Cryptosystem in the context
 * of the additive group of integers modulo 41 (ℤ/41ℤ)
 *
 * @author Frank Zeyda (frank.zeyda@freeandfair.us)
 * @copyright Free & Fair 2025-26
 * @version 0.1
 */
module Primitives::ElGamal::Inst::ElGamal_Z41_Test where

import Primitives::ElGamal::Inst::ElGamal_Z41
import Algebra::Encoders::Inst::Encoder_Z41
import Algebra::Encoders::Inst::EncoderUtils_Z41

/**
 * Simple testing function encrypting and decrypting five messages.
 */
main : [5][bits]
main = tv'dec
  where tv = [0, 1, 2, 5, 31]
        tv'enc = map Strict'Encode tv
        tv'run = map (gen_enc_dec 1 2) tv'enc
        tv'dec = map Strict'Decode tv'run

/**
 * Simple testing function encrypting and decrypting five messages
 * while producing additional trace output.
 */
main'trace : [5][bits]
main'trace = traceVal "test result:" tv'dec
  where tv = traceVal "test vector:" [0, 1, 2, 5, 31]
        tv'enc = map Strict'Encode tv
        tv'run = map (gen_enc_dec 1 2) tv'enc
        tv'dec = map Strict'Decode tv'run
